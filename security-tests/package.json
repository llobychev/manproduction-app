{
  "name": "manclub-firestore-security-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase --config ../firebase.json emulators:exec --project manproduction-club-rules-test --only firestore 'node --test firestore.rules.test.mjs'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.2.1",
    "firebase-tools": "^14.12.1"
  }
}

# MenClub application versions

- `v1/` — immutable stable application loaded from commit `abbab1139e4eeeea40f977ab4e34753ee0ace103`.
- `v2/` — independently evolving six-tab application.
- `active.json` — launcher activation manifest. Default is `v1`.

Use `../app.html?version=v1` or `../app.html?version=v2` for explicit testing.
